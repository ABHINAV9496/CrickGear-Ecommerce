# Copy this file to .env and fill in your own values.
# The frontend is served by Vite; only VITE_ prefixed variables reach the browser.

# Base URL of the Django backend (no trailing /api)
VITE_API_URL=http://localhost:8000

# Google OAuth client ID (used for "Continue with Google" login)
VITE_GOOGLE_CLIENT_ID=
